# ─────────────────────────────────────────────────────────────
# GitLab AI Chatbot — Production Dependencies
# Used by Render.com deployment (api.py + FastAPI + Pinecone)
#
# Intentionally excludes:
#   - torch          (not needed; sentence-transformers uses CPU ONNX)
#   - faiss-cpu      (replaced by Pinecone in production)
#   - streamlit      (standalone UI, not part of FastAPI deployment)
# ─────────────────────────────────────────────────────────────

# ── Web Server ──────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-dotenv>=1.0.0

# ── LangChain Core ──────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# ── LLM: Google Gemini ──────────────────────────────────────
langchain-google-genai>=2.0.0
google-generativeai>=0.8.0
google-ai-generativelanguage>=0.6.0

# ── Embeddings (CPU-only, no torch needed) ──────────────────
langchain-huggingface>=0.3.0
sentence-transformers>=3.0.0
huggingface-hub>=0.20.0
tokenizers>=0.19.0
safetensors>=0.4.0

# ── Vector DB: Pinecone ─────────────────────────────────────
langchain-pinecone>=0.2.0
pinecone-client>=3.0.0

# ── Utilities ───────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.24.0
requests>=2.28.0
numpy>=1.24.0
scikit-learn>=1.3.0
tqdm>=4.65.0
PyYAML>=6.0
tenacity>=8.0.0
